import { Repository } from 'typeorm';
import { StockAlert } from './entities/stock-alert.entity';
import { Company } from 'src/company/entities/company.entity';
import { Model } from 'src/models/entities/model.entity';
import { AllPart } from 'src/all-parts/entities/all-part.entity';
import { Reference } from 'src/references/entities/reference.entity';
import { StockPart } from 'src/stock-parts/entities/stock-part.entity';
import { Bin } from 'src/bin/entities/bin.entity';
import { Branch } from 'src/branches/entities/branch.entity';
import { User } from 'src/users/entities/user.entity';
import { Repair } from 'src/repair/entities/repair.entity';
export declare class StockAlertService {
    private stockAlertRepo;
    private companyRepo;
    private modelRepo;
    private allPartRepo;
    private referenceRepo;
    private stockPartRepo;
    private binRepo;
    private branchRepo;
    private userRepo;
    private repairRepo;
    constructor(stockAlertRepo: Repository<StockAlert>, companyRepo: Repository<Company>, modelRepo: Repository<Model>, allPartRepo: Repository<AllPart>, referenceRepo: Repository<Reference>, stockPartRepo: Repository<StockPart>, binRepo: Repository<Bin>, branchRepo: Repository<Branch>, userRepo: Repository<User>, repairRepo: Repository<Repair>);
    getAlerts(branchId: number, userId: number, type?: string): Promise<{
        isRead: boolean;
        id: number;
        branchId: number;
        type: string;
        report: Record<string, any>[];
        readBy: string[];
        createdAt: Date;
    }[]>;
    markAsRead(alertId: number, userId: number): Promise<StockAlert | null>;
    generateAlertForBranch(branchId: number): Promise<StockAlert | null>;
    generateForAllBranches(): Promise<StockAlert[]>;
    generateReceptionAlertForBranch(branchId: number): Promise<StockAlert | null>;
    generateReceptionForAllBranches(): Promise<StockAlert[]>;
    generateAffectationAlertForBranch(branchId: number): Promise<StockAlert | null>;
    generateAffectationForAllBranches(): Promise<StockAlert[]>;
    generateReparationAlertForBranch(branchId: number): Promise<StockAlert | null>;
    generateReparationForAllBranches(): Promise<StockAlert[]>;
    generateCqAlertForBranch(branchId: number): Promise<StockAlert | null>;
    generateCqForAllBranches(): Promise<StockAlert[]>;
    generateBloqueAlertForBranch(branchId: number): Promise<StockAlert | null>;
    generateBloqueForAllBranches(): Promise<StockAlert[]>;
    weeklyStockAlert(): Promise<StockAlert[]>;
    dailyReceptionAlert(): Promise<void>;
    dailyAffectationAlert(): Promise<void>;
    dailyReparationAlert(): Promise<void>;
    dailyCqAlert(): Promise<void>;
    dailyBloqueAlert(): Promise<void>;
    findAlertById(id: number): Promise<StockAlert | null>;
    getAlertUsers(branchId: number): Promise<User[]>;
}
